#!/bin/sh
set -eu

if ! command -v golangci-lint >/dev/null 2>&1; then
  echo "golangci-lint is required for server checks: https://golangci-lint.run/usage/install/"
  exit 1
fi

cd "Zestaw 5/server"
golangci-lint run --config .golangci.yml ./...
