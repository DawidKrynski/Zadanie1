server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback — all unmatched routes serve index.html for react-router to handle
    location / {
        try_files $uri $uri/ /index.html;
    }
}
